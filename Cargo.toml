[package]
name = "paxor"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }  
serde = { version = "1", features = ["derive"] } 
wincode = { version = "0.4", features = ["derive"] }
anyhow = "1.0"                         